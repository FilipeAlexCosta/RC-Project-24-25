#include "scoreboard.hpp"
#include <iostream>


/*void ScoreBoard::add_game(game& g) {
    if (g.has_ended() != game::result::WON)
        return;
    scoreboard.insert(g);
    if (scoreboard.size() > MAX_TOP_SCORES)
        scoreboard.erase(--scoreboard.end());
}

void ScoreBoard::print_sb_test() {
    int rank = 1;
    for (const auto& g: scoreboard) {
        std::cout << "" << rank << " - " << g.current_trial() << " " << g.secret_key() << '\n';
    }
}*/
